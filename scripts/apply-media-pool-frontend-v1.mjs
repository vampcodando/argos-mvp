import fs from "node:fs";
import path from "node:path";

const ROOT = process.cwd();
const COMPONENT_PATH = path.join(ROOT, "src", "components", "MasterChatHome.tsx");
const INDEX_CSS_PATH = path.join(ROOT, "src", "index.css");
const MOBILE_CSS_PATH = path.join(ROOT, "src", "mobile.css");
const MARKER = "ARGOS_MEDIA_POOL_FRONTEND_V1";

function fail(message) {
  console.error(`\n[ARGOS MEDIA POOL] ERRO: ${message}\n`);
  process.exit(1);
}

function read(filePath) {
  if (!fs.existsSync(filePath)) {
    fail(`Arquivo não encontrado: ${filePath}`);
  }
  return fs.readFileSync(filePath, "utf8");
}

function replaceOnce(source, anchor, replacement, label) {
  const first = source.indexOf(anchor);
  if (first < 0) {
    fail(`Âncora não encontrada (${label}). Nenhum arquivo foi gravado.`);
  }
  const second = source.indexOf(anchor, first + anchor.length);
  if (second >= 0) {
    fail(`Âncora duplicada (${label}). Nenhum arquivo foi gravado.`);
  }
  return source.slice(0, first) + replacement + source.slice(first + anchor.length);
}

const originalComponent = read(COMPONENT_PATH);
const originalIndexCss = read(INDEX_CSS_PATH);
const originalMobileCss = read(MOBILE_CSS_PATH);

if (
  originalComponent.includes(MARKER) &&
  originalIndexCss.includes(MARKER) &&
  originalMobileCss.includes(MARKER)
) {
  console.log("[ARGOS MEDIA POOL] Frontend V1 já está aplicado. Nenhuma alteração necessária.");
  process.exit(0);
}

if (
  originalComponent.includes(MARKER) ||
  originalIndexCss.includes(MARKER) ||
  originalMobileCss.includes(MARKER)
) {
  fail("Aplicação parcial detectada. Restaure os arquivos do backup antes de executar novamente.");
}

let component = originalComponent;

component = replaceOnce(
  component,
  `type ChatMessage = {\n  id: string;\n  role: "master" | "user";\n  text: string;\n  status?: "normal" | "loading" | "error";\n  label?: string;\n  imageBase64?: string;\n  imageMimeType?: string;\n};`,
  `// ${MARKER}\ntype MediaKind = "image" | "video";\ntype MediaStatus = "generating" | "succeeded" | "failed";\n\ntype MediaMessage = {\n  kind: MediaKind;\n  status: MediaStatus;\n  url?: string;\n  taskId?: string;\n  prompt?: string;\n  size?: string;\n  resolution?: string;\n  ratio?: string;\n  duration?: number;\n  error?: string;\n};\n\ntype ChatMessage = {\n  id: string;\n  role: "master" | "user";\n  text: string;\n  status?: "normal" | "loading" | "error";\n  label?: string;\n  imageBase64?: string;\n  imageMimeType?: string;\n  media?: MediaMessage;\n};`,
  "tipos ChatMessage"
);

component = replaceOnce(
  component,
  `  const [attachments, setAttachments] = useState<PendingAttachment[]>([]);\n  const [attachmentNotice, setAttachmentNotice] = useState("");`,
  `  const [attachments, setAttachments] = useState<PendingAttachment[]>([]);\n  const [attachmentNotice, setAttachmentNotice] = useState("");\n  const [mediaPanelOpen, setMediaPanelOpen] = useState(false);\n  const [mediaKind, setMediaKind] = useState<MediaKind>("image");\n  const [mediaImageSize, setMediaImageSize] = useState("2K");\n  const [mediaVideoRatio, setMediaVideoRatio] = useState("16:9");\n  const [mediaVideoResolution, setMediaVideoResolution] = useState("480p");\n  const [mediaVideoDuration, setMediaVideoDuration] = useState(5);\n  const [mediaVideoAudio, setMediaVideoAudio] = useState(false);\n  const [mediaReferenceUrls, setMediaReferenceUrls] = useState<string[]>([]);`,
  "estados Media Pool"
);

const mediaFunctions = `  async function submitMediaRequest(\n    kind: MediaKind,\n    prompt: string,\n    attachmentsForRequest: PendingAttachment[]\n  ) {\n    if (!prompt.trim()) {\n      setAttachmentNotice("Descreva o que o ARGOS deve gerar antes de enviar.");\n      return;\n    }\n\n    const nonImageAttachments = attachmentsForRequest.filter(\n      (attachment) => !isCloudImageAttachment(attachment.file)\n    );\n\n    if (nonImageAttachments.length) {\n      setAttachmentNotice(\n        "No modo Mídia, use apenas imagens como referência. Remova documentos ou ZIPs antes de gerar."\n      );\n      return;\n    }\n\n    const imageAttachments = attachmentsForRequest.filter(\n      (attachment) => isCloudImageAttachment(attachment.file)\n    );\n\n    if (imageAttachments.length > 2) {\n      setAttachmentNotice(\n        "No Media Pool V1, use no máximo 2 imagens de referência por geração."\n      );\n      return;\n    }\n\n    const loadingId = createId();\n    const controller = new AbortController();\n    abortRef.current = controller;\n\n    const userMessage: ChatMessage = {\n      id: createId(),\n      role: "user",\n      text: prompt,\n    };\n\n    const initialMedia: MediaMessage = {\n      kind,\n      status: "generating",\n      prompt,\n      resolution: kind === "video" ? mediaVideoResolution : undefined,\n      ratio: kind === "video" ? mediaVideoRatio : undefined,\n      duration: kind === "video" ? mediaVideoDuration : undefined,\n      size: kind === "image" ? mediaImageSize : undefined,\n    };\n\n    setMessages((current) => [\n      ...current,\n      userMessage,\n      {\n        id: loadingId,\n        role: "master",\n        label: "ARGOS — Media Pool",\n        text:\n          kind === "image"\n            ? "Gerando imagem no Media Pool..."\n            : "Iniciando geração de vídeo no Media Pool...",\n        status: "loading",\n        media: initialMedia,\n      },\n    ]);\n\n    setDraft("");\n    setSending(true);\n    setActiveLoadingId(loadingId);\n    setMediaPanelOpen(false);\n    setAttachmentNotice("");\n\n    const savedReferenceUrls = [...mediaReferenceUrls];\n    setMediaReferenceUrls([]);\n\n    try {\n      let preparedAttachmentUrls: string[] = [];\n\n      if (imageAttachments.length) {\n        updateLoadingMessage(\n          loadingId,\n          imageAttachments.length === 1\n            ? "Preparando imagem de referência para o Media Pool..."\n            : "Preparando imagens de referência para o Media Pool..."\n        );\n\n        const prepared = await Promise.all(\n          imageAttachments.map((attachment) =>\n            prepareCloudImage(attachment.file)\n          )\n        );\n\n        preparedAttachmentUrls = prepared.map((image) => image.dataUrl);\n      }\n\n      const referenceImages = [\n        ...savedReferenceUrls,\n        ...preparedAttachmentUrls,\n      ].slice(0, 2);\n\n      const requestBody =\n        kind === "image"\n          ? {\n              mediaType: "image",\n              prompt,\n              size: mediaImageSize,\n              images: referenceImages,\n              watermark: false,\n              dataClass: "creative_asset",\n            }\n          : {\n              mediaType: "video",\n              prompt,\n              images: referenceImages,\n              ratio: mediaVideoRatio,\n              resolution: mediaVideoResolution,\n              duration: mediaVideoDuration,\n              generateAudio: mediaVideoAudio,\n              draft: mediaVideoResolution === "480p",\n              dataClass: "creative_asset",\n            };\n\n      const response = await fetch("/api/media/generate", {\n        method: "POST",\n        headers: {\n          "content-type": "application/json",\n          accept: "application/json",\n        },\n        signal: controller.signal,\n        body: JSON.stringify(requestBody),\n      });\n\n      const payload = await response.json();\n\n      if (!response.ok || !payload?.ok) {\n        throw new Error(\n          payload?.reason ||\n            payload?.error?.message ||\n            "Falha ao gerar mídia no ARGOS."\n        );\n      }\n\n      if (kind === "image") {\n        const imageUrl = String(payload?.image?.url || "").trim();\n\n        if (!imageUrl) {\n          throw new Error("O Media Pool respondeu sem URL da imagem.");\n        }\n\n        updateLoadingMessage(\n          loadingId,\n          payload?.fallbackUsed\n            ? "Imagem gerada pelo Media Pool com fallback gratuito."\n            : "Imagem gerada pelo Media Pool.",\n          "normal",\n          {\n            label: "ARGOS — Media Pool",\n            media: {\n              kind: "image",\n              status: "succeeded",\n              url: imageUrl,\n              prompt,\n              size: payload?.image?.size || mediaImageSize,\n            },\n          }\n        );\n        setAttachments([]);\n        return;\n      }\n\n      const taskId = String(payload?.task?.id || "").trim();\n\n      if (!taskId) {\n        throw new Error("O Media Pool respondeu sem task id do vídeo.");\n      }\n\n      updateLoadingMessage(\n        loadingId,\n        "Vídeo em processamento...",\n        "loading",\n        {\n          label: "ARGOS — Media Pool",\n          media: {\n            ...initialMedia,\n            taskId,\n          },\n        }\n      );\n\n      for (let attempt = 0; attempt < 90; attempt += 1) {\n        await new Promise((resolve) => window.setTimeout(resolve, 2000));\n\n        const statusResponse = await fetch(\n          "/api/media/video-status?id=" + encodeURIComponent(taskId),\n          {\n            method: "GET",\n            cache: "no-store",\n            headers: { accept: "application/json" },\n            signal: controller.signal,\n          }\n        );\n\n        const statusPayload = await statusResponse.json();\n\n        if (!statusResponse.ok || !statusPayload?.ok) {\n          throw new Error(\n            statusPayload?.reason || "Falha ao consultar o vídeo gerado."\n          );\n        }\n\n        const task = statusPayload.task || {};\n        const taskStatus = String(task.status || "").toLowerCase();\n\n        if (taskStatus === "succeeded") {\n          const videoUrl = String(task.videoUrl || "").trim();\n\n          if (!videoUrl) {\n            throw new Error("O vídeo concluiu sem URL de reprodução.");\n          }\n\n          updateLoadingMessage(\n            loadingId,\n            "Vídeo concluído pelo Media Pool.",\n            "normal",\n            {\n              label: "ARGOS — Media Pool",\n              media: {\n                kind: "video",\n                status: "succeeded",\n                url: videoUrl,\n                taskId,\n                prompt,\n                resolution: task.resolution || mediaVideoResolution,\n                ratio: task.ratio || mediaVideoRatio,\n                duration: task.duration || mediaVideoDuration,\n              },\n            }\n          );\n          setAttachments([]);\n          return;\n        }\n\n        if (["failed", "error", "cancelled", "canceled"].includes(taskStatus)) {\n          throw new Error(\n            task?.error?.message || "A geração do vídeo falhou no provedor."\n          );\n        }\n\n        updateLoadingMessage(\n          loadingId,\n          taskStatus === "queued"\n            ? "Vídeo na fila do Media Pool..."\n            : "Vídeo em processamento...",\n          "loading",\n          {\n            label: "ARGOS — Media Pool",\n            media: {\n              ...initialMedia,\n              taskId,\n            },\n          }\n        );\n      }\n\n      throw new Error("Tempo limite atingido aguardando o vídeo.");\n    } catch (error) {\n      const cancelled =\n        error instanceof DOMException && error.name === "AbortError";\n      const reason = cancelled\n        ? "Geração de mídia cancelada pelo usuário."\n        : error instanceof Error\n          ? error.message\n          : "Erro desconhecido no Media Pool.";\n\n      updateLoadingMessage(\n        loadingId,\n        reason,\n        "error",\n        {\n          label: "ARGOS — Media Pool",\n          media: {\n            ...initialMedia,\n            status: "failed",\n            error: reason,\n          },\n        }\n      );\n    } finally {\n      if (abortRef.current === controller) {\n        abortRef.current = null;\n      }\n      setSending(false);\n      setActiveLoadingId(null);\n    }\n  }\n\n`;

component = replaceOnce(
  component,
  `  async function handleSubmit() {\n    if (sending) {`,
  mediaFunctions + `  async function handleSubmit() {\n    if (sending) {`,
  "função submitMediaRequest"
);

component = replaceOnce(
  component,
  `    if (!value && !attachmentsForRequest.length) {\n      return;\n    }\n\n    const imageAttachments = attachmentsForRequest.filter(`,
  `    if (!value && !attachmentsForRequest.length) {\n      return;\n    }\n\n    if (mediaPanelOpen) {\n      await submitMediaRequest(mediaKind, value, attachmentsForRequest);\n      return;\n    }\n\n    const imageAttachments = attachmentsForRequest.filter(`,
  "roteamento manual para Media Pool"
);

component = replaceOnce(
  component,
  `          >\n            Enviar arquivo\n          </button>\n\n          <p className="master-attachment-help">`,
  `          >\n            Enviar arquivo\n          </button>\n\n          <button\n            type="button"\n            className={\`master-attachment-button master-media-toggle \${\n              mediaPanelOpen ? "is-active" : ""\n            }\`}\n            onClick={() => setMediaPanelOpen((current) => !current)}\n            disabled={sending}\n            aria-expanded={mediaPanelOpen}\n          >\n            Mídia {mediaPanelOpen ? "▲" : ""}\n          </button>\n\n          <p className="master-attachment-help">`,
  "botão Mídia"
);

const mediaPanelJsx = `        {mediaPanelOpen ? (\n          <section className="master-media-panel" aria-label="Configuração do Media Pool">\n            <div className="master-media-panel-head">\n              <div>\n                <strong>Media Pool</strong>\n                <span>gratuito · fail-closed · sem fallback pago automático</span>\n              </div>\n              <button\n                type="button"\n                className="master-media-close"\n                onClick={() => setMediaPanelOpen(false)}\n                disabled={sending}\n                aria-label="Fechar painel de mídia"\n              >\n                ×\n              </button>\n            </div>\n\n            <div className="master-media-tabs" role="tablist" aria-label="Tipo de mídia">\n              <button\n                type="button"\n                className={mediaKind === "image" ? "is-active" : ""}\n                onClick={() => setMediaKind("image")}\n                disabled={sending}\n              >\n                Imagem\n              </button>\n              <button\n                type="button"\n                className={mediaKind === "video" ? "is-active" : ""}\n                onClick={() => setMediaKind("video")}\n                disabled={sending}\n              >\n                Vídeo\n              </button>\n            </div>\n\n            {mediaReferenceUrls.length ? (\n              <div className="master-media-reference-chip">\n                <span>1 referência HTTPS ativa</span>\n                <button\n                  type="button"\n                  onClick={() => setMediaReferenceUrls([])}\n                  disabled={sending}\n                >\n                  remover\n                </button>\n              </div>\n            ) : null}\n\n            {mediaKind === "image" ? (\n              <div className="master-media-options master-media-options-image">\n                <label>\n                  <span>Qualidade</span>\n                  <select\n                    value={mediaImageSize}\n                    onChange={(event) => setMediaImageSize(event.target.value)}\n                    disabled={sending}\n                  >\n                    <option value="2K">2K</option>\n                    <option value="4K">4K</option>\n                  </select>\n                </label>\n              </div>\n            ) : (\n              <div className="master-media-options">\n                <label>\n                  <span>Formato</span>\n                  <select\n                    value={mediaVideoRatio}\n                    onChange={(event) => setMediaVideoRatio(event.target.value)}\n                    disabled={sending}\n                  >\n                    <option value="16:9">16:9</option>\n                    <option value="9:16">9:16</option>\n                    <option value="1:1">1:1</option>\n                    <option value="4:3">4:3</option>\n                    <option value="3:4">3:4</option>\n                  </select>\n                </label>\n\n                <label>\n                  <span>Duração</span>\n                  <select\n                    value={mediaVideoDuration}\n                    onChange={(event) => setMediaVideoDuration(Number(event.target.value))}\n                    disabled={sending}\n                  >\n                    <option value={5}>5 segundos</option>\n                    <option value={8}>8 segundos</option>\n                    <option value={10}>10 segundos</option>\n                    <option value={12}>12 segundos</option>\n                  </select>\n                </label>\n\n                <label>\n                  <span>Qualidade</span>\n                  <select\n                    value={mediaVideoResolution}\n                    onChange={(event) => setMediaVideoResolution(event.target.value)}\n                    disabled={sending}\n                  >\n                    <option value="480p">480p · econômico</option>\n                    <option value="720p">720p</option>\n                    <option value="1080p">1080p</option>\n                  </select>\n                </label>\n\n                <label>\n                  <span>Áudio</span>\n                  <select\n                    value={mediaVideoAudio ? "yes" : "no"}\n                    onChange={(event) => setMediaVideoAudio(event.target.value === "yes")}\n                    disabled={sending}\n                  >\n                    <option value="no">Sem áudio</option>\n                    <option value="yes">Com áudio</option>\n                  </select>\n                </label>\n              </div>\n            )}\n\n            <p className="master-media-hint">\n              Use o mesmo campo de mensagem abaixo como prompt. Imagens anexadas podem servir como referência.\n            </p>\n          </section>\n        ) : null}\n\n`;

component = replaceOnce(
  component,
  `        <textarea\n          value={draft}`,
  mediaPanelJsx + `        <textarea\n          value={draft}`,
  "painel inline antes do textarea"
);

const mediaCardJsx = `            {message.media ? (\n              <div className={\`master-media-card master-media-card-\${message.media.kind}\`}>\n                {message.media.kind === "image" && message.media.url ? (\n                  <img\n                    className="master-media-result"\n                    src={message.media.url}\n                    alt="Imagem gerada pelo Media Pool do ARGOS"\n                  />\n                ) : null}\n\n                {message.media.kind === "video" &&\n                message.media.status === "succeeded" &&\n                message.media.url ? (\n                  <video\n                    className="master-media-result master-media-video"\n                    src={message.media.url}\n                    controls\n                    playsInline\n                    preload="metadata"\n                  />\n                ) : null}\n\n                {message.media.status === "generating" ? (\n                  <div className="master-media-generating" role="status">\n                    <span className="master-media-spinner" aria-hidden="true" />\n                    <strong>\n                      {message.media.kind === "video"\n                        ? "Gerando vídeo..."\n                        : "Gerando imagem..."}\n                    </strong>\n                  </div>\n                ) : null}\n\n                <div className="master-media-card-meta">\n                  <span>{message.media.kind === "image" ? "Imagem" : "Vídeo"}</span>\n                  {message.media.size ? <span>{message.media.size}</span> : null}\n                  {message.media.resolution ? <span>{message.media.resolution}</span> : null}\n                  {message.media.ratio ? <span>{message.media.ratio}</span> : null}\n                  {message.media.duration ? <span>{message.media.duration}s</span> : null}\n                </div>\n\n                {message.media.status === "succeeded" ? (\n                  <div className="master-media-card-actions">\n                    {message.media.url ? (\n                      <a\n                        href={message.media.url}\n                        target="_blank"\n                        rel="noreferrer"\n                      >\n                        Abrir mídia\n                      </a>\n                    ) : null}\n\n                    <button\n                      type="button"\n                      onClick={() => {\n                        setMediaKind(message.media?.kind || "image");\n                        setDraft(message.media?.prompt || "");\n                        setMediaPanelOpen(true);\n                      }}\n                      disabled={sending}\n                    >\n                      Criar variação\n                    </button>\n\n                    {message.media.kind === "image" && message.media.url ? (\n                      <button\n                        type="button"\n                        onClick={() => {\n                          setMediaReferenceUrls([message.media?.url || ""].filter(Boolean));\n                          setMediaKind("video");\n                          setMediaPanelOpen(true);\n                          setAttachmentNotice(\n                            "Imagem gerada selecionada como referência HTTPS para a próxima mídia."\n                          );\n                        }}\n                        disabled={sending}\n                      >\n                        Usar como referência\n                      </button>\n                    ) : null}\n                  </div>\n                ) : null}\n              </div>\n            ) : null}\n`;

component = replaceOnce(
  component,
  `            {message.imageBase64 ? (\n              <img\n                className="master-chat-image-result"\n                src={\`data:\${message.imageMimeType || "image/png"};base64,\${message.imageBase64}\`}\n                alt="Imagem gerada pelo ARGOS"\n              />\n            ) : null}\n`,
  `            {message.imageBase64 ? (\n              <img\n                className="master-chat-image-result"\n                src={\`data:\${message.imageMimeType || "image/png"};base64,\${message.imageBase64}\`}\n                alt="Imagem gerada pelo ARGOS"\n              />\n            ) : null}\n` + mediaCardJsx,
  "card de mídia no histórico"
);

const desktopCss = `\n\n/* ${MARKER} START */\n.master-attachment-controls {\n  align-items: flex-start;\n}\n\n.master-media-toggle.is-active {\n  border-color: color-mix(in srgb, var(--accent) 72%, var(--border));\n  background: color-mix(in srgb, var(--accent) 16%, transparent);\n  color: var(--accent);\n}\n\n.master-media-panel {\n  width: 100%;\n  box-sizing: border-box;\n  margin: 0.65rem 0;\n  padding: 0.8rem;\n  border: 1px solid color-mix(in srgb, var(--accent) 35%, var(--border));\n  border-radius: 0.9rem;\n  background: color-mix(in srgb, var(--panel) 82%, var(--bg));\n  display: grid;\n  gap: 0.7rem;\n}\n\n.master-media-panel-head {\n  display: flex;\n  align-items: flex-start;\n  justify-content: space-between;\n  gap: 0.75rem;\n}\n\n.master-media-panel-head > div {\n  display: grid;\n  gap: 0.2rem;\n}\n\n.master-media-panel-head strong {\n  color: var(--fg);\n}\n\n.master-media-panel-head span,\n.master-media-hint {\n  color: var(--muted);\n  font-size: 0.72rem;\n}\n\n.master-media-close {\n  width: 32px;\n  height: 32px;\n  border-radius: 999px;\n  border: 1px solid var(--border);\n  background: transparent;\n  color: var(--fg);\n  cursor: pointer;\n}\n\n.master-media-tabs {\n  display: grid;\n  grid-template-columns: repeat(2, minmax(0, 1fr));\n  gap: 0.45rem;\n}\n\n.master-media-tabs button,\n.master-media-card-actions button,\n.master-media-card-actions a,\n.master-media-reference-chip button {\n  border: 1px solid var(--border);\n  border-radius: 999px;\n  background: color-mix(in srgb, var(--bg) 72%, transparent);\n  color: var(--fg);\n  padding: 0.5rem 0.72rem;\n  cursor: pointer;\n  text-decoration: none;\n  font: inherit;\n}\n\n.master-media-tabs button.is-active {\n  border-color: color-mix(in srgb, var(--accent) 72%, var(--border));\n  color: var(--accent);\n  background: color-mix(in srgb, var(--accent) 13%, transparent);\n}\n\n.master-media-options {\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: 0.6rem;\n}\n\n.master-media-options-image {\n  grid-template-columns: minmax(150px, 240px);\n}\n\n.master-media-options label {\n  min-width: 0;\n  display: grid;\n  gap: 0.3rem;\n}\n\n.master-media-options label > span {\n  color: var(--muted);\n  font-size: 0.72rem;\n}\n\n.master-media-options select {\n  width: 100%;\n  min-width: 0;\n  box-sizing: border-box;\n  color: var(--fg);\n  background: var(--input-bg);\n  border: 1px solid var(--input-border);\n  border-radius: 0.65rem;\n  padding: 0.55rem 0.65rem;\n}\n\n.master-media-hint {\n  margin: 0;\n}\n\n.master-media-reference-chip {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 0.5rem;\n  padding: 0.45rem 0.6rem;\n  border-radius: 0.7rem;\n  border: 1px solid color-mix(in srgb, var(--accent) 32%, var(--border));\n  color: var(--muted);\n}\n\n.master-media-card {\n  width: 100%;\n  max-width: 100%;\n  box-sizing: border-box;\n  margin-top: 0.7rem;\n  padding: 0.65rem;\n  border-radius: 0.9rem;\n  border: 1px solid color-mix(in srgb, var(--accent) 32%, var(--border));\n  background: color-mix(in srgb, var(--bg) 62%, transparent);\n  overflow: hidden;\n}\n\n.master-media-result {\n  display: block;\n  width: 100%;\n  max-width: 100%;\n  height: auto;\n  max-height: 64vh;\n  object-fit: contain;\n  border-radius: 0.7rem;\n  background: #000;\n}\n\n.master-media-video {\n  aspect-ratio: 16 / 9;\n}\n\n.master-media-generating {\n  min-height: 84px;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 0.7rem;\n  color: var(--muted);\n}\n\n.master-media-spinner {\n  width: 18px;\n  height: 18px;\n  border: 2px solid color-mix(in srgb, var(--accent) 25%, transparent);\n  border-top-color: var(--accent);\n  border-radius: 999px;\n  animation: argos-media-spin 0.85s linear infinite;\n}\n\n@keyframes argos-media-spin {\n  to { transform: rotate(360deg); }\n}\n\n.master-media-card-meta,\n.master-media-card-actions {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 0.45rem;\n  align-items: center;\n  margin-top: 0.55rem;\n}\n\n.master-media-card-meta span {\n  color: var(--muted);\n  font-size: 0.72rem;\n  border: 1px solid var(--border);\n  border-radius: 999px;\n  padding: 0.25rem 0.45rem;\n}\n/* ${MARKER} END */\n`;

const mobileCss = `\n\n/* ${MARKER} START */\n@media (max-width: 820px), ((max-width: 1024px) and (pointer: coarse)) {\n  .master-chat-composer {\n    max-height: min(58dvh, 520px);\n    overflow-y: auto;\n    overflow-x: hidden;\n    overscroll-behavior: contain;\n  }\n\n  .master-attachment-controls {\n    display: grid;\n    grid-template-columns: repeat(2, minmax(0, auto));\n    gap: 0.45rem;\n    align-items: start;\n  }\n\n  .master-attachment-help {\n    grid-column: 1 / -1;\n    min-width: 0;\n    overflow-wrap: anywhere;\n  }\n\n  .master-media-panel {\n    margin: 0.5rem 0;\n    padding: 0.7rem;\n  }\n\n  .master-media-tabs {\n    grid-template-columns: repeat(2, minmax(0, 1fr));\n  }\n\n  .master-media-options,\n  .master-media-options-image {\n    grid-template-columns: 1fr;\n  }\n\n  .master-media-options select {\n    min-height: 42px;\n    font-size: 1rem;\n  }\n\n  .master-media-result {\n    width: 100%;\n    max-width: 100%;\n    max-height: 44dvh;\n  }\n\n  .master-media-card-actions {\n    display: grid;\n    grid-template-columns: repeat(2, minmax(0, 1fr));\n  }\n\n  .master-media-card-actions > * {\n    min-width: 0;\n    width: 100%;\n    box-sizing: border-box;\n    text-align: center;\n  }\n}\n\n@media (max-width: 390px) {\n  .master-attachment-controls {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .master-attachment-button {\n    width: 100%;\n    min-width: 0;\n  }\n\n  .master-media-card-actions {\n    grid-template-columns: 1fr;\n  }\n}\n/* ${MARKER} END */\n`;

const indexCss = originalIndexCss.trimEnd() + desktopCss + "\n";
const mobileCssOutput = originalMobileCss.trimEnd() + mobileCss + "\n";

const timestamp = new Date().toISOString().replace(/[:.]/g, "-");
const backupDir = path.join(ROOT, "backups", `media-pool-frontend-v1-${timestamp}`);
fs.mkdirSync(backupDir, { recursive: true });
fs.writeFileSync(path.join(backupDir, "MasterChatHome.tsx"), originalComponent, "utf8");
fs.writeFileSync(path.join(backupDir, "index.css"), originalIndexCss, "utf8");
fs.writeFileSync(path.join(backupDir, "mobile.css"), originalMobileCss, "utf8");

fs.writeFileSync(COMPONENT_PATH, component, "utf8");
fs.writeFileSync(INDEX_CSS_PATH, indexCss, "utf8");
fs.writeFileSync(MOBILE_CSS_PATH, mobileCssOutput, "utf8");

console.log("[ARGOS MEDIA POOL] Frontend V1 aplicado com sucesso.");
console.log(`[ARGOS MEDIA POOL] Backup local: ${path.relative(ROOT, backupDir)}`);
console.log("[ARGOS MEDIA POOL] Próximo passo: npm run build");
